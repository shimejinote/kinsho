'use client';

/**
 * Former full-frame ink well for the manuscript page.
 * Constellation mode keeps the void visible — blackout stays off.
 */
export default function ProseBlackout() {
  return null;
}
